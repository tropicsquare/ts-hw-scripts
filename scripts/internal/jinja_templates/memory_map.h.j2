{%- macro format_line(name, value) -%}
    {{ "#define {:58}    0x{:08X}".format(name, value) }}
{%- endmacro %}
/*******************************************************************************
* @file {{ header.filename }}
* @author Tropic Square
* @brief {{ header.filename.stem }} top base address definitions
* @note Autogenerated by TS Memory Map Generator at {{ date.strftime("%I:%M%p on %B %d, %Y") }}
*
* @license TODO
*******************************************************************************/

#ifndef {{ header.header_name | upper }}
#define {{ header.header_name | upper }}

{% for tuple_list in defines -%}
    {%- for name, value in tuple_list -%}
        {{ format_line(name, value) }}
    {% endfor %}
{% endfor -%}

#endif // ! {{ header.header_name | upper }}
